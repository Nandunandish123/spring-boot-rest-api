spring:
  datasource:
    username: root
    password: root@123
    url: jdbc:mysql://localhost:3306/spring_boot_training
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true